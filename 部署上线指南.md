# 东北农虾品牌网站 · 部署与收录指南

网站已做好 SEO 优化(关键词 / Open Graph / 结构化数据 JSON-LD / canonical / sitemap / robots),共 7 个页面。下面按顺序操作即可上线。

---

## 第 0 步:确定主域名并替换占位符(必做)

全站代码里域名写的是占位符 `__BASE__`(在 canonical、og:image、JSON-LD、robots、sitemap 中)。先部署到任一平台,拿到真实网址后,用一条命令批量替换:

```bash
cd 东北农虾品牌网站
node tools/seo.js --set-url https://你的域名/
```

> 示例:`node tools/seo.js --set-url https://dongbei-nongxia.vercel.app/`
> 三个平台都会生成不同域名,建议 **选择一个做主站**(如 Vercel),canonical 都指向它,另两个正常可访问即可。

---

## 方式 A:GitHub Pages(免费、无需备案)

1. 注册/登录 [GitHub](https://github.com)
2. 新建仓库,名称建议 `dongbei-nongxia`,选择 **Public**
3. 本机推送(仓库已初始化好,只需关联远程):
   ```bash
   cd 东北农虾品牌网站
   git remote add origin https://github.com/你的用户名/dongbei-nongxia.git
   git branch -M main
   git push -u origin main
   ```
4. 打开仓库 → **Settings → Pages** → Source 选 **Deploy from a branch** → 分支 `main`、目录 `/ (root)` → Save
5. 等待 1—2 分钟,访问 `https://你的用户名.github.io/dongbei-nongxia/index.html`

> 注意:GitHub Pages 子域名是 `用户名.github.io/仓库名`,能访问但比较长。爬虫收录没问题。

---

## 方式 B:Vercel(免费、无需备案、推荐做主站)

1. 注册/登录 [Vercel](https://vercel.com)(可用 GitHub 账号一键登录)
2. 点击 **Add New → Project** → **Import** 你刚推送到 GitHub 的 `dongbei-nongxia` 仓库
3. Framework Preset 选 **Other**(纯静态),Build Command 留空,Output Directory 留空 → Deploy
4. 几分钟后得到 `https://dongbei-nongxia-xxx.vercel.app`,可直接访问
5. 可在项目 **Settings → Domains** 把网址改短(免费子域名)

---

## 方式 C:腾讯云开发 CloudBase 静态托管(国内访问快)

前置:需要腾讯云账号 + 微信扫码登录。

1. 安装 CLI(如未安装):
   ```bash
   npm install -g @cloudbase/cli
   ```
2. 登录(会弹出二维码,用微信扫):
   ```bash
   cloudbase login
   ```
3. 创建环境并获取环境 ID:
   ```bash
   cloudbase env:create 东北农虾
   # 输出里的 Environment ID(形如 xxx-xxxxxxxx)记下来
   ```
4. 把 `cloudbaserc.json` 里的 `envId` 改成你的环境 ID
5. 部署(把当前目录上传为静态托管):
   ```bash
   cloudbase hosting deploy . -e 你的环境ID
   ```
6. 完成后在腾讯云开发控制台 → 静态网站托管 → 获得 `https://xxx.tcloudbaseapp.com` 访问地址

> 说明:CloudBase 默认域名免备案。若以后绑定自己的正式域名且用国内服务器,需 ICP 备案(约 10—20 天)。

---

## 第 3 步:提交搜索引擎收录(上线后立即做)

### 百度(国内最重要,务必提交)
1. 打开 [百度搜索资源平台](https://ziyuan.baidu.com),用百度账号登录
2. **用户中心 → 站点管理 → 添加站点** → 输入你的网址(如 `https://dongbei-nongxia-xxx.vercel.app`)
3. 完成站点验证(有 HTML 标签 / CNAME 两种方式,选 HTML 标签把 meta 加到首页即可)
4. **普通收录 → sitemap** → 提交 `你的域名/sitemap.xml`
5. **链接提交 → 手动提交** → 把 7 个页面网址逐个贴进去
6. **索引量 → 死链检测** 可定期查看收录情况

> 新站提示:百度对新站有"考察期",通常 1—4 周后陆续收录。坚持更新内容 + 外部有链入会更快。

### 谷歌
1. 打开 [Google Search Console](https://search.google.com/search-console),用 Google 账号登录
2. 添加资源 → 属性类型选 **网址前缀**,填你的网址
3. 验证后,左侧 **站点地图** 提交 `sitemap.xml`
4. 谷歌收录通常 1—3 天,可用"URL 检查"手动请求收录

### 必应(Bing)
1. 打开 [Bing Webmaster Tools](https://www.bing.com/webmasters),可用微软账号登录
2. 添加站点并验证(推荐直接导入 Google Search Console)
3. 提交 `sitemap.xml`

---

## 收录后如何让搜索排名更好(关键词建议)

- 优先让**首页**被搜到:`东北农虾`、`寒地小龙虾`、`稻田小龙虾`
- 品牌词(东北农虾)收录后很快能排第一;品类词(寒地小龙虾、即食小龙虾)需要时间积累
- 加速手段:把网址发到朋友圈/微信群/小红书/知乎,只要有外部链接指向网站,爬虫会更快发现并加深权重
- 小程序、公众号里放官网链接同样有效

---

## 常见问题

| 问题 | 说明 |
|---|---|
| 为什么百度搜不到? | 新站收录需要时间(1—4 周),且务必先在百度资源平台提交并验证 |
| 免费子域名会影响排名吗? | 有正式域名更好,但子域名也能被收录。建议 Vercel 做主站 |
| 页面会不会因为 JS 动画不被爬虫收录? | 内容都在 HTML 里(非 JS 渲染),爬虫可正常抓取;动画只是 CSS 层 |
| 联系方式的占位符 | `400-XXX-XXXX`、`cooperation@dbnx.example` 记得替换成真实信息后再部署 |
